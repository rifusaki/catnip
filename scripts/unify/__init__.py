"""Dataset unification CLI wrappers.

Each module in this package is a thin wrapper around the importable
parsers in :mod:`src.preprocess`.  The wrappers handle CLI argument
parsing, logging, and summary printing — all parsing and I/O logic lives
in the library code.
"""
